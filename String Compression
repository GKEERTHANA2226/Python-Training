s = input()
result = ""
count = 1
for i in range(len(s) - 1):
    if s[i] == s[i + 1]:
        count += 1
    else:
        result += str(count) + s[i]
        count = 1
result += str(count) + s[-1]

print(result)
